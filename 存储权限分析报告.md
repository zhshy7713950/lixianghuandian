# 存储权限分析报告

经过对全项目代码的检索与分析，以下是关于 `WRITE_EXTERNAL_STORAGE` 和 `READ_EXTERNAL_STORAGE` 两个权限的详细分析报告：

### 1. 写入文件权限：`android.permission.WRITE_EXTERNAL_STORAGE`

#### 申请权限的意图
项目中在多处申请了该权限，主要意图包括：
1. **APP 版本更新下载**：`AtyMain.kt` 中下载新版本 APK 到本地缓存目录 `externalCacheDir`。
2. **图片压缩与处理**：`ZipImg.kt` 在用户上传图片前，将压缩后的图片写入到应用专属缓存目录。
3. **合同电子签名**：`FgtContractSignStep2.kt` 中将用户绘制的电子签名保存为本地图片。
4. **自定义相机拍照**：`CameraActivity.kt`（身份证拍照）拍摄后将图片保存到 SD 卡根目录下的公共文件夹 `IdCard` 中。
5. **微信分享海报**：`CreateShareActivity.java` 生成包含二维码的分享图片并保存到本地以供调用微信分享。
6. **退租设备拍照**：`FgtReturn.kt` 中退租时初始化相册选择器。
7. **第三方 SDK 需求**：`AndroidManifest.xml` 中高德地图和推送 SDK 声明了该权限用于可能的地图数据缓存等。

#### 分析申请的页面是否还有可能使用
**是的，仍然在使用。** 上述涉及的页面（如首页更新、实名认证、合同签约、退租设备、海报分享等）均是该 APP 的核心业务流程，这些页面的相关逻辑依然会在用户日常操作中被触发。

#### 分析是否有必要申请
**绝大部分场景已无必要申请，仅有一处遗留技术债导致需要该权限。**
- **无必要的场景**：自 Android 4.4 (API 19) 起，应用向其专属目录（如 `getExternalCacheDir()` 或 `getExternalFilesDir()`）写入文件时，**不再需要**申请 `WRITE_EXTERNAL_STORAGE` 权限。本项目 `build.gradle` 中配置的 `minSdkVersion` 为 23。因此，在 `AtyMain.kt`、`ZipImg.kt`、`FgtContractSignStep2.kt` 以及 `CreateShareActivity.java` 中往专属缓存目录写文件的行为，申请该权限属于多余操作。
- **唯一必要的场景**：在自定义身份证相机 `CustomCameraPreview.java` 中，图片保存路径被硬编码到了外部存储的公共根目录（`Environment.getExternalStorageDirectory() + "/IdCard"`）。往公共存储根目录写文件在旧版本 Android 中严格需要写入权限（且在 Android 10+ 开启分区存储后会直接报错）。
- **优化建议**：如果将上述身份证相机的保存路径改为 `context.getExternalFilesDir()` 或 `context.getExternalCacheDir()`，则全项目都可以**彻底移除**动态申请 `WRITE_EXTERNAL_STORAGE` 权限的代码。

---

### 2. 读取文件权限：`android.permission.READ_EXTERNAL_STORAGE`

#### 申请权限的意图
1. **相册图片选择**：`FgtTrueName.kt`（实名认证）、`FgtContractSignStep2.kt` 使用第三方库（`PictureSelectorHelper` 或 `Matisse`）打开系统相册供用户选择身份证或人脸照片。
2. **WebView 文件上传**：`DefaultFileChooserStrategy.kt` 中网页前端触发 `<input type="file">` 时，调起系统文件/图片选择器。
3. **通用资源访问**：在 `CreateShareActivity` 与 `ZipImg` 等流程中配合写入权限一并申请，以确保拥有完整的读写权限。

#### 分析申请的页面是否还有可能使用
**是的，仍然在使用。** WebView 的文件上传支持以及实名认证的相册选图都是业务上仍在使用的高频功能。

#### 分析是否有必要申请
**部分场景当前实现下有必要，但可以通过现代化改造完全避免申请。**
- **有必要的场景**：目前项目中使用的 `PictureSelector` 和 `Matisse` 等第三方相册库，在通过 `MediaStore` 扫描并向用户展示相册全部图片列表时，在 Android 12 及以下版本确实需要 `READ_EXTERNAL_STORAGE` 权限（Android 13+ 则细分为 `READ_MEDIA_IMAGES` 等）。因此在现有的技术选型下，相册选图页面申请此权限是必要的。
- **无必要的场景**：在 WebView 的 `DefaultFileChooserStrategy.kt` 中，使用的是系统原生的 `Intent.ACTION_PICK`（系统选择器）来选择图片。系统自带的选择器返回给 App 的 `Uri` 会自动附带临时读取权限（`FLAG_GRANT_READ_URI_PERMISSION`），App 完全可以直接通过这个 Uri 读取该文件而无需事先申请任何权限。这里的权限申请是冗余的。
- **优化建议**：随着 Android 隐私政策收紧，可以考虑升级相册选择库，或者使用 Android 官方推荐的 **Photo Picker (照片选择器)** / `ActivityResultContracts.GetContent()`。这些系统级方案由系统界面完成图片选择，App 只需被动接收目标文件的临时 Uri 读写权，这样整个项目就不再需要申请广泛申请读取设备相册的权限了。
